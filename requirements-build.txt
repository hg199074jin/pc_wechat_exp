# WeChat EXP — minimal dependencies for PyInstaller build
# Core (always required)
pycryptodome>=3.20.0
flask>=3.0.0
zstandard>=0.22.0
blackboxprotobuf>=1.0.0
requests>=2.28.0
PySocks>=1.7.1

# Scheduled AI analysis / knowledge scans
apscheduler>=3.10.0

# Knowledge export to Word
python-docx>=1.1.0

# Manual generation (README.md → HTML)
markdown>=3.5.0

# Feature-specific (wordcloud, employee export, wrapped report)
jieba>=0.42.1
openpyxl>=3.1.0
pypinyin>=0.49.0

# Build tool
pyinstaller>=6.0.0
